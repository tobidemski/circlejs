import build from './build.min'

export default Object.assign(build, {
    input: 'build/imports.js',
    output: Object.assign(build.output, {
        file: 'dist/circle.esm.min.js',
        format: 'es'
    })
})